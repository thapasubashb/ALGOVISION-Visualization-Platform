// Knowledge base entry for the chatbot — Normalization (DBMS)
export const content = "Normalization (DBMS)\n====================\n\nWhat it is:\nNormalization is the process of organizing tables to reduce data duplication and avoid update anomalies, by progressively enforcing rules called normal forms (1NF, 2NF, 3NF, and beyond).\n\nWhy it matters:\nAn unnormalized or under-normalized table duplicates data (like an instructor's name repeated for every student in their course). That duplication means one fact can go out of sync — update the instructor in one row and forget another, and now the data contradicts itself.\n\nHow it works:\n1NF removes repeating groups so every cell holds one atomic value. 2NF removes partial dependencies (a non-key column depending on only part of a composite key). 3NF removes transitive dependencies (a non-key column depending on another non-key column instead of the key itself).\n\nWhat to observe:\nWatch which column gets highlighted as duplicated right before each split, and how that exact column moves into its own table in the next step — the highlight is always the reason for the following decomposition.\n\nOutcome:\nThree clean 3NF tables — Student, Course, and Enrollment — where every fact is stored exactly once.\n\nImportant points:\n- Each step exists to fix one specific kind of duplication — normalization isn't an arbitrary set of rules, it's a direct response to a data anomaly.\n- Fully normalized schemas avoid update/insert/delete anomalies, but often need JOINs to reassemble the original view — a tradeoff against pure read speed.\n- Real systems sometimes deliberately denormalize (undo some of this) for read-heavy workloads, once they understand the tradeoff.\n\nComplexity / performance:\nNormalization is a design-time process, not a runtime algorithm — but its payoff is at runtime: it turns an O(n) \"update every duplicate row\" operation into an O(1) update of a single row.\n\nReal-world relevance:\nAlmost any real schema — e-commerce orders/products, social media posts/tags, this very AlgoVision app's users/progress tables — follows these same normalization principles to avoid duplicated, inconsistent data.\n\n"
