// Knowledge base entry for the chatbot — Disk Scheduling (Operating Systems)
export const content = "Disk Scheduling (Operating Systems)\n===================================\n\nWhat it is:\nDisk scheduling algorithms decide the order in which pending disk I/O requests (each targeting a specific track) are serviced, to minimize the total distance the physical disk head has to travel.\n\nWhy it matters:\nOn mechanical (spinning) disks, moving the read/write head is by far the slowest part of an I/O operation. Servicing requests in a smart order rather than arrival order can dramatically reduce total seek time and improve throughput.\n\nHow it works:\nFCFS ignores position entirely. SSTF always jumps to the nearest request. SCAN sweeps across the whole disk in one direction like an elevator, servicing everything along the way, then reverses. C-SCAN does the same sweep but always jumps back to the start rather than reversing, keeping wait times more uniform.\n\nWhat to observe:\nSwitch algorithms on the identical request queue and head start position, and compare total head movement directly — notice how FCFS's \"zig-zag\" path (chasing requests in arrival order) covers far more distance than the others.\n\nOutcome:\nThe same 8 requests get serviced by every algorithm, but the total head travel distance differs substantially depending on which strategy is used.\n\nImportant points:\n- SSTF can starve far-away requests if closer ones keep arriving — similar to the starvation risk in SJF/priority CPU scheduling.\n- SCAN and C-SCAN provide more predictable, uniform wait times than SSTF, at some cost to raw total movement in certain cases.\n- On modern SSDs, there's no physical head to move, so these algorithms matter far less than they did (and still do) for spinning hard disks.\n\nComplexity / performance:\nFCFS is O(1) per request (no sorting needed). SSTF costs O(n) per pick without a sorted structure. SCAN/C-SCAN sort the queue once, O(n log n), then service it in a single pass.\n\nReal-world relevance:\nDatabase engines and OS I/O schedulers historically used variants of these algorithms (like the Linux \"elevator\" I/O scheduler, directly named after SCAN's elevator analogy) to optimize spinning-disk performance.\n\n"
