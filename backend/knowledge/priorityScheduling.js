// Knowledge base entry for the chatbot — Priority Scheduling (Operating Systems)
export const content = "Priority Scheduling (Operating Systems)\n=======================================\n\nWhat it is:\nPriority Scheduling assigns each process a priority value and always runs the highest-priority process among those currently ready, rather than simply the earliest arrival or shortest job.\n\nWhy it matters:\nNot all work is equally urgent — a real-time system alarm should run before a background log-cleanup task, regardless of which one technically arrived first. Priority scheduling lets the system express that urgency directly.\n\nHow it works:\nWhenever the CPU becomes free, the scheduler scans all arrived, unfinished processes and picks the one with the best (commonly, lowest-numbered) priority. In the non-preemptive version shown here, once selected a process always runs to completion.\n\nWhat to observe:\nWatch P4 (priority 1) jump ahead of P3 (priority 2) even though P3 arrived earlier — priority, not arrival order, is what determines the schedule here.\n\nOutcome:\nHigher-priority processes systematically get better (lower) waiting times than lower-priority ones, at the cost of potentially making low-priority processes wait a long time.\n\nImportant points:\n- Priority scheduling can starve low-priority processes indefinitely if higher-priority ones keep arriving — a serious real-world risk.\n- \"Aging\" is a common fix for starvation: a process's priority gradually improves the longer it waits, guaranteeing it eventually runs.\n- SJF is actually a special case of priority scheduling where priority = burst time (shorter burst = higher priority).\n\nComplexity / performance:\nSelecting the highest-priority ready process is O(log n) with a priority queue, or O(n) with a simple linear scan — either way, cheap compared to the actual work being scheduled.\n\nReal-world relevance:\nReal-time operating systems (used in cars, medical devices, industrial control) rely heavily on priority scheduling — a safety-critical task must always preempt a routine one.\n\n"
