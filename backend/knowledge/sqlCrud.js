// Knowledge base entry for the chatbot — SQL Queries & CRUD (DBMS)
export const content = "SQL Queries & CRUD (DBMS)\n=========================\n\nWhat it is:\nCRUD stands for Create, Read, Update, Delete — the four basic operations SQL provides for managing data: CREATE TABLE defines structure, INSERT adds rows, SELECT reads rows, UPDATE modifies rows, and DELETE removes rows.\n\nWhy it matters:\nAlmost every feature of every application eventually reduces to one of these operations against a database — signing up a user is an INSERT, viewing a profile is a SELECT, editing it is an UPDATE, and deleting an account is a DELETE.\n\nHow it works:\nEach statement names a table and, for UPDATE/DELETE/SELECT, a WHERE clause that filters which rows are affected. The engine scans (or uses an index to jump to) the matching rows and applies the operation to exactly those rows — never more, never less.\n\nWhat to observe:\nWatch how INSERT adds a whole new row, UPDATE changes only the flashing cell while leaving the rest of the row alone, and DELETE removes the entire row rather than just a value.\n\nOutcome:\nA table that ends up with 2 rows out of the original 3 inserted, one of them with a modified salary — reflecting exactly the sequence of statements that ran.\n\nImportant points:\n- Forgetting a WHERE clause on UPDATE or DELETE applies the change to every row in the table — a classic, dangerous mistake.\n- SELECT never modifies data; it only reads and optionally filters/sorts it.\n- The order operations run in matters — this DELETE only succeeds because it runs after the row already exists from an earlier INSERT.\n\nComplexity / performance:\nWithout an index, INSERT is O(1) (append), while SELECT/UPDATE/DELETE with a WHERE clause are O(n) since every row must be checked against the condition.\n\nReal-world relevance:\nEvery ORM (Django ORM, Sequelize, Hibernate, Prisma) you'll use as a developer is ultimately just generating these same four statements on your behalf.\n\n"
