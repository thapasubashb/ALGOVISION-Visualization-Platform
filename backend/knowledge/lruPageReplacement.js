// Knowledge base entry for the chatbot — LRU Page Replacement (Operating Systems)
export const content = "LRU Page Replacement (Operating Systems)\n========================================\n\nWhat it is:\nLRU (Least Recently Used) page replacement evicts whichever resident page has gone the longest without being accessed, when a new page must be loaded and no frame is free.\n\nWhy it matters:\nPrograms tend to exhibit \"locality of reference\" — pages used recently are likely to be used again soon, and pages untouched for a while are less likely to be needed. LRU directly exploits this pattern, unlike FIFO which ignores usage entirely.\n\nHow it works:\nThe OS tracks an ordering of resident pages by how recently each was accessed. On a hit, the accessed page moves to the \"most recently used\" end. On a fault with no free frames, the page at the \"least recently used\" end is evicted.\n\nWhat to observe:\nCompare directly against FIFO on the identical reference string — watch page 0 survive longer under LRU (because it keeps getting re-accessed) than it did under FIFO's pure load-order eviction.\n\nOutcome:\nLRU produces fewer page faults than FIFO on this same reference string, because it evicts based on actual usage recency rather than arbitrary load order.\n\nImportant points:\n- True LRU needs a timestamp or ordered list update on every single memory access, which is more expensive to implement in hardware than FIFO's simple queue.\n- Real systems often use an approximation of LRU (like the \"clock\" / second-chance algorithm) that's cheaper to track but behaves similarly.\n- LRU does not suffer from Bélády's anomaly the way FIFO can — adding more frames to LRU never increases the fault count.\n\nComplexity / performance:\nA correct O(1) implementation is possible using a doubly-linked list plus a hash map (moving a page to the \"most recent\" end in constant time) — the same design used in many LRU-cache implementations.\n\nReal-world relevance:\nLRU (or an approximation of it) is the eviction policy behind not just OS page replacement, but also CPU caches, browser caches, CDN caches, and libraries like Python's `functools.lru_cache`.\n\n"
