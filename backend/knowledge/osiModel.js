// Knowledge base entry for the chatbot — OSI Model (Computer Networks)
export const content = "OSI Model (Computer Networks)\n=============================\n\nWhat it is:\nThe OSI (Open Systems Interconnection) model divides network communication into 7 conceptual layers, each responsible for one aspect of getting data from one application to another across a network.\n\nWhy it matters:\nSplitting networking into layers lets each one be designed, implemented, and replaced independently — Wi-Fi and Ethernet differ at the Physical/Data Link layers, but everything above them (like an HTTP request) doesn't need to know or care which one is in use.\n\nHow it works:\nAt the sender, each layer wraps the data from the layer above it with its own header (encapsulation), from Application down to Physical. At the receiver, each layer reads and strips off its corresponding header in reverse order (decapsulation), until the original data reaches the receiving application.\n\nWhat to observe:\nWatch the data unit's name change at specific layers — Data → Segment → Packet → Frame → Bits — and notice that the receiver's layers are visited in the exact reverse order of the sender's.\n\nOutcome:\nThe receiving application ends up with the exact same data the sending application started with, having passed through 14 total layer-crossings (7 down, 7 up) along the way.\n\nImportant points:\n- Only 4 of the 7 layers actually change the data unit's name (Transport/Network/Data Link/Physical) — Application, Presentation and Session all still just carry \"Data\".\n- Real-world protocol stacks (like TCP/IP) often combine several OSI layers into one — see the TCP/IP Model topic for that comparison.\n- Each layer only talks to its direct peer layer on the other end conceptually, even though physically everything travels through the layers below it.\n\nComplexity / performance:\nEncapsulation/decapsulation adds a fixed, small, constant overhead per layer (a header of some tens of bytes) — it doesn't scale with the size of the actual data being sent.\n\nReal-world relevance:\nEvery single web request, video call, or file download you've ever made has been encapsulated and decapsulated through layers just like this, even though you never see any of it happen.\n\n"
