// Knowledge base entry for the chatbot — Keys & Constraints (DBMS)
export const content = "Keys & Constraints (DBMS)\n=========================\n\nWhat it is:\nKeys and constraints are rules the database enforces on every write: PRIMARY KEY (unique, non-null row identifier), FOREIGN KEY (must reference an existing row elsewhere), UNIQUE, NOT NULL, and CHECK (a custom boolean condition on a value).\n\nWhy it matters:\nWithout constraints, invalid data (duplicate IDs, missing required fields, impossible values) can silently enter the database, corrupting every feature built on top of it later. Constraints reject bad data at the moment it's written, which is far cheaper than cleaning it up afterward.\n\nHow it works:\nOn every INSERT or UPDATE, the engine checks each declared constraint in turn. The first constraint that fails aborts the entire statement — none of it is applied, not even partially.\n\nWhat to observe:\nWatch how each failing insert stops at a different stage depending on what's actually wrong with it, and how a row that fails any single check leaves the table completely untouched.\n\nOutcome:\nOnly the one fully valid row makes it into the table; every invalid attempt is rejected with the table left exactly as it was before.\n\nImportant points:\n- A PRIMARY KEY is both UNIQUE and NOT NULL by definition — it's really two constraints in one.\n- CHECK constraints are the only ones in this example that look at the actual value rather than just presence/uniqueness.\n- Constraint checks are typically cheap because PRIMARY KEY and UNIQUE columns are automatically indexed — checking for a duplicate is an index lookup, not a full table scan.\n\nComplexity / performance:\nA PK/UNIQUE check against an indexed column is roughly O(log n); a CHECK constraint on a single value being inserted is O(1) since it only examines that one row.\n\nReal-world relevance:\nThat \"email already in use\" error you see when signing up for almost any website is a UNIQUE constraint violation being caught and turned into a friendly message by the application.\n\n"
