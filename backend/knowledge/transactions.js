// Knowledge base entry for the chatbot — Transactions & ACID (DBMS)
export const content = "Transactions & ACID (DBMS)\n==========================\n\nWhat it is:\nA transaction is a group of one or more database operations that must all succeed together or all fail together — there's no partial outcome. ACID (Atomicity, Consistency, Isolation, Durability) names the four guarantees a proper transaction provides.\n\nWhy it matters:\nReal operations often need multiple writes to stay correct — like moving money, which requires both a debit and a credit. If only one side of that happened, data would become permanently wrong (money created or destroyed). Transactions prevent that.\n\nHow it works:\nChanges are staged in a private workspace during BEGIN/READ/WRITE, invisible to everyone else. COMMIT makes them all permanent at once; ROLLBACK discards them all, leaving the database exactly as it was before the transaction started.\n\nWhat to observe:\nWatch how the \"committed\" values never change until COMMIT happens, and how ROLLBACK snaps the pending value back to the last committed value rather than leaving it half-applied.\n\nOutcome:\nT1 successfully transfers 200 (A=800, B=700, permanently). T2 attempts to transfer 300 but fails partway and rolls back completely — the final state is unaffected by T2's aborted attempt.\n\nImportant points:\n- Atomicity: all-or-nothing — T2's partial withdrawal from A was fully undone, not left in place.\n- Consistency: the total (A+B) is preserved by every committed transaction — value is neither created nor destroyed.\n- Isolation: while T1 was mid-transaction, nobody else could see its uncommitted 800/700 values.\n- Durability: once T1 committed, its result survives — it doesn't depend on the transaction still being \"in progress\".\n\nComplexity / performance:\nNot an algorithmic complexity in the Big-O sense — the cost of ACID guarantees is typically paid in locking/logging overhead, which is why transactions should be kept as short as reasonably possible.\n\nReal-world relevance:\nBank transfers, e-commerce checkouts (deduct stock + charge card + create order), and airline seat booking all rely on transactions to avoid the exact \"money disappears\" or \"double-booked seat\" bugs this simulation illustrates.\n\n"
