// Knowledge base entry for the chatbot — Concurrency Control (DBMS)
export const content = "Concurrency Control (DBMS)\n==========================\n\nWhat it is:\nConcurrency control is the set of techniques a database uses to let multiple transactions run at the same time without corrupting each other's work — most commonly using locks.\n\nWhy it matters:\nIf two transactions read the same value and both write back their own update, one update can silently overwrite the other (a \"lost update\"), even though each transaction individually looked correct. Real systems have many transactions running simultaneously, so this has to be handled automatically.\n\nHow it works:\nBefore modifying a row, a transaction acquires a lock on it. Any other transaction wanting the same lock must wait until it's released (typically at COMMIT or ROLLBACK). This serializes access to that specific row without blocking transactions touching unrelated data.\n\nWhat to observe:\nWatch T2 sit in a genuine \"waiting\" state — not proceeding, not erroring — until T1's lock is released, and notice T2 reads T1's committed value (600), never T1's uncommitted in-progress one.\n\nOutcome:\nBoth updates apply correctly and in order: 500 → 600 (T1) → 650 (T2), with no lost update, at the cost of T2 having to wait briefly.\n\nImportant points:\n- Locking trades some concurrency (T2 has to wait) for correctness (no lost updates) — this is a fundamental tradeoff, not a flaw.\n- Real databases often use row-level locks (as shown here) rather than table-level locks, so unrelated rows aren't affected.\n- Holding locks for too long (long transactions) increases how often other transactions have to wait — a common real-world performance issue.\n\nComplexity / performance:\nAcquiring/releasing an uncontended lock is O(1); a waiting transaction's delay depends entirely on how long the lock holder takes, not on data size.\n\nReal-world relevance:\nTwo people trying to book the last seat on a flight, or two bank tellers processing withdrawals from the same account at the same moment, are exactly this scenario in production systems.\n\n"
