// Knowledge base entry for the chatbot — DNS Resolution (Computer Networks)
export const content = "DNS Resolution (Computer Networks)\n==================================\n\nWhat it is:\nDNS (Domain Name System) is the hierarchical, distributed system that translates human-readable domain names (like www.example.com) into the IP addresses computers actually use to connect.\n\nWhy it matters:\nIP addresses are hard to remember and can change over time. DNS lets services keep a stable, memorable name while the underlying IP address (and even server infrastructure) changes freely behind the scenes.\n\nHow it works:\nA resolver walks a hierarchy: Root DNS servers know only which server handles each top-level domain (.com, .org, etc); TLD servers know which server is authoritative for a specific domain; the Authoritative server holds the actual records and gives the final answer.\n\nWhat to observe:\nCompare the two runs — count how many hops the uncached lookup takes versus the cached one, and notice the cached answer skips straight from Resolver to Browser.\n\nOutcome:\nBoth lookups end with the browser learning 93.184.216.34, but the cached lookup gets there in a fraction of the steps.\n\nImportant points:\n- Every DNS record has a TTL (time-to-live) that controls how long resolvers are allowed to cache it before checking again.\n- The \"Root → TLD → Authoritative\" chain is why DNS scales globally — no single server needs to know every domain in the world.\n- DNS caching happens at multiple levels — your browser, your OS, your router, and your ISP's resolver can all cache the same answer independently.\n\nComplexity / performance:\nAn uncached lookup takes a small constant number of hops (typically 3–4) regardless of how many domains exist worldwide, thanks to the hierarchical structure — it's effectively O(1) in practice, not O(n) over all domains.\n\nReal-world relevance:\nEvery single web address you've ever typed into a browser triggered a DNS resolution like this one before your browser could load anything.\n\n"
