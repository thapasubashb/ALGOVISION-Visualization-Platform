// Knowledge base entry for the chatbot — FCFS CPU Scheduling (Operating Systems)
export const content = "FCFS CPU Scheduling (Operating Systems)\n=======================================\n\nWhat it is:\nFirst-Come, First-Served (FCFS) is the simplest CPU scheduling algorithm: processes are executed strictly in the order they arrive, with no preemption once a process starts running.\n\nWhy it matters:\nIt's the most straightforward, fair-in-a-naive-sense way to schedule — everyone gets served in line, just like a queue at a counter. It's a useful baseline to compare every other scheduling algorithm against.\n\nHow it works:\nProcesses are sorted by arrival time. The scheduler picks the earliest arrival first and runs it completely, uninterrupted, before considering the next process in line — even if a much shorter job arrives moments later.\n\nWhat to observe:\nWatch P3 (burst 2) and P4 (burst 1) — both short jobs — sit waiting behind P1's long 5-unit burst, purely because of arrival order, not because it's efficient.\n\nOutcome:\nAll 4 processes complete in arrival order, with a noticeably higher average waiting time than a smarter algorithm (like SJF) would achieve on the same workload.\n\nImportant points:\n- FCFS is simple to implement (a plain FIFO queue) but can produce the \"convoy effect\" — short processes stuck waiting behind one long one.\n- FCFS is non-preemptive: once a process starts, nothing can interrupt it, even a much more urgent, shorter task.\n- Average waiting time under FCFS depends heavily on arrival order, not on burst time — a bad arrival order can hurt overall throughput badly.\n\nComplexity / performance:\nEach scheduling decision is O(1) using a simple queue; overall runtime to schedule n processes is O(n).\n\nReal-world relevance:\nBasic batch-processing systems and simple print-job queues often use FCFS-like scheduling — simplicity matters more than optimal turnaround time in those contexts.\n\n"
