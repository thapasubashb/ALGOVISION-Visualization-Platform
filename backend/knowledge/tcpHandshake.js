// Knowledge base entry for the chatbot — TCP 3-Way Handshake (Computer Networks)
export const content = "TCP 3-Way Handshake (Computer Networks)\n=======================================\n\nWhat it is:\nThe TCP 3-way handshake is the sequence of three segments (SYN, SYN-ACK, ACK) that two hosts exchange to open a reliable, connection-oriented TCP session before any application data is sent.\n\nWhy it matters:\nTCP promises reliable, in-order, duplicate-free delivery. To keep that promise both ends must first agree on starting sequence numbers and confirm that each side can actually hear the other — the handshake establishes that shared state.\n\nHow it works:\nThe client sends a SYN with its initial sequence number. The server replies with a single segment carrying both SYN (its own sequence number) and ACK (acknowledging the client). The client finally sends an ACK for the server's sequence number, and both sides move to ESTABLISHED.\n\nWhat to observe:\nWatch the state label under each box change (CLOSED → SYN-SENT/LISTEN → SYN-RECEIVED → ESTABLISHED) exactly as each packet arrives, not before — a state only changes once the corresponding segment has actually been received.\n\nOutcome:\nBoth client and server end in the ESTABLISHED state, each aware of the other's starting sequence number, ready to exchange data reliably.\n\nImportant points:\n- The SYN-ACK is one segment doing two jobs — acknowledging the client and proposing the server's own sequence number.\n- The final ACK needs no reply of its own, which is why the client can consider the connection open one step before the server does.\n- Sequence numbers here are simplified round numbers; real TCP picks a randomized initial sequence number (ISN) for security.\n\nComplexity / performance:\nThe handshake always takes exactly 3 segments and roughly 1.5 round trips, regardless of how much data will eventually be sent — this fixed cost is why protocols like TLS 1.3 and QUIC try to reduce or overlap it.\n\nReal-world relevance:\nEvery HTTP(S) request, SSH session, and database connection over TCP starts with this exact exchange. Tools like Wireshark show these same three packets at the start of any TCP stream capture.\n\n"
