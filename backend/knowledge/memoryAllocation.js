// Knowledge base entry for the chatbot — Memory Allocation (Operating Systems)
export const content = "Memory Allocation (Operating Systems)\n=====================================\n\nWhat it is:\nMemory allocation strategies decide which free block of memory to give a process when it requests space, when multiple free blocks are large enough.\n\nWhy it matters:\nOver time, as processes allocate and free memory, available space becomes fragmented into scattered blocks of different sizes. Which block gets chosen for a new request significantly affects how much usable free space remains later.\n\nHow it works:\nFirst Fit takes the first block encountered that's big enough. Best Fit scans everything and picks the smallest sufficient block (minimizing leftover waste). Worst Fit scans everything and picks the largest block (trying to leave a more useful-sized remainder).\n\nWhat to observe:\nSwitch between the three strategies on the identical block/process setup and watch how differently they behave — notably, whether P4 (426 KB) can be allocated at all depends entirely on which strategy was used earlier.\n\nOutcome:\nBest Fit successfully allocates all 4 processes on this specific example, while First Fit and Worst Fit both leave P4 unable to find a large-enough block — a direct, concrete illustration of how allocation strategy affects real outcomes.\n\nImportant points:\n- No single strategy is universally best — Best Fit's full-block scan is slower, and can create many small, useless leftover fragments over time (despite the name).\n- This scenario is a \"worst case\" specifically chosen to show fragmentation — real workloads vary widely in how each strategy performs.\n- Compaction (shifting allocated memory together to merge free space into one large block) is one way operating systems combat fragmentation, at the cost of the time it takes to move things.\n\nComplexity / performance:\nFirst Fit averages O(n) per allocation (early exit possible). Best Fit and Worst Fit are O(n) per allocation too, but always scan every block since they need the true minimum/maximum.\n\nReal-world relevance:\nThese exact strategies (and their tradeoffs) apply directly to how `malloc` implementations, memory pool allocators, and even disk space allocators decide where to place new data.\n\n"
