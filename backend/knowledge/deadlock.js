// Knowledge base entry for the chatbot — Deadlocks (DBMS)
export const content = "Deadlocks (DBMS)\n================\n\nWhat it is:\nA deadlock is a cycle of transactions each waiting for a resource held by the next one in the cycle, so none of them can ever proceed.\n\nWhy it matters:\nLocking (see Concurrency Control) prevents lost updates, but it introduces this new risk: if two transactions grab resources in opposite orders, they can each end up waiting on the other forever.\n\nHow it works:\nThe database tracks a \"wait-for graph\" — a directed edge from transaction X to Y means X is blocked waiting for a resource Y holds. If that graph ever contains a cycle, a deadlock exists. Periodic detection scans for such cycles, and recovery aborts one transaction (the \"victim\") to break the cycle.\n\nWhat to observe:\nWatch the exact moment the second wait-for edge appears — that's the instant the cycle forms and the deadlock becomes real, even though the detector doesn't notice until the next scan.\n\nOutcome:\nT2 is aborted and rolled back (releasing Resource B), which lets T1 finally acquire everything it needs and complete. T2 would typically be retried by the application afterward.\n\nImportant points:\n- A deadlock can only be broken by aborting at least one participant — waiting longer never helps once a true cycle exists.\n- Victim selection usually tries to minimize wasted work (e.g. abort the transaction that has done the least so far, or holds the fewest locks).\n- A simple prevention strategy is to always acquire locks in a globally consistent order — if every transaction locks A before B, this particular deadlock shape becomes impossible.\n\nComplexity / performance:\nCycle detection in a wait-for graph with n transactions is O(n + e) using a standard graph traversal (e = number of wait edges) — cheap even though the consequence (aborting work) is costly.\n\nReal-world relevance:\nYou've likely seen \"deadlock detected, transaction rolled back\" errors from MySQL or PostgreSQL — this is the database automatically doing exactly the detection and recovery shown here.\n\n"
