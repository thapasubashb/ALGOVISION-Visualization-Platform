// Knowledge base entry for the chatbot — IP Addressing (Computer Networks)
export const content = "IP Addressing (Computer Networks)\n=================================\n\nWhat it is:\nAn IPv4 address is a 32-bit number, almost always written as four 8-bit octets in decimal, separated by dots (dotted-decimal notation) — e.g. 192.168.1.10.\n\nWhy it matters:\nEvery device that communicates over IP needs a unique address so packets know where to go — and that address needs to encode both \"which network\" and \"which device on that network,\" similar to how a postal address encodes both a city and a street number.\n\nHow it works:\nA prefix length (like /24) or subnet mask specifies how many of the 32 bits are the network portion. Bits before the prefix boundary must match for two devices to be on the same network; the remaining bits identify individual hosts within it.\n\nWhat to observe:\nWatch each decimal octet convert to its 8-bit binary form, and see exactly where the /24 boundary falls — right at the edge between the 3rd and 4th octet, which is why /24 addresses are so common (it lines up neatly with octet boundaries).\n\nOutcome:\nThe address 192.168.1.10/24 is understood as network 192.168.1.0 with host number 10 — meaning up to 254 usable host addresses exist on this specific network.\n\nImportant points:\n- Not all prefix lengths line up neatly on an octet boundary — /26, for example, splits a byte in half, which is exactly what the Subnetting topic explores.\n- 192.168.x.x, 10.x.x.x, and 172.16.x.x–172.31.x.x are reserved private address ranges, which is why 192.168.1.10 is such a common address on home routers.\n- IPv4 has only about 4.3 billion possible addresses, which is why IPv6 (128-bit addresses) exists — but IPv4 remains dominant in most home/office networks.\n\nComplexity / performance:\nConverting between decimal and binary, and computing the network/host split, are all O(1) — fixed 32-bit arithmetic regardless of network size.\n\nReal-world relevance:\nConfiguring a home router, setting up a static IP on a server, or reading a `ping`/`ipconfig`/`ifconfig` output all require exactly this decimal ↔ binary, network/host understanding.\n\n"
