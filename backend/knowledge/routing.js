// Knowledge base entry for the chatbot — Routing (Computer Networks)
export const content = "Routing (Computer Networks)\n===========================\n\nWhat it is:\nRouting is the process of forwarding a packet from its source to its destination across multiple networks, hop by hop, using routing tables at each router along the way.\n\nWhy it matters:\nThe internet is made of an enormous number of interconnected networks — no single device has (or needs) a complete map of the entire path. Routing lets each router make a local, independent decision that collectively gets the packet to the right place.\n\nHow it works:\nEach router examines the packet's destination IP address, looks it up against its own routing table (matching the destination network), and forwards the packet to the corresponding \"next hop\" — another router or the final destination if directly connected.\n\nWhat to observe:\nWatch how each router only ever decides its own single next hop — none of them knows or needs to know the full path from Host A to Host B.\n\nOutcome:\nThe packet reaches Host B after 3 independent forwarding decisions, none of which required global knowledge of the network.\n\nImportant points:\n- Routing tables are usually built automatically by routing protocols (like OSPF or BGP) rather than configured by hand for every possible destination.\n- A route with a more specific prefix (e.g. /24 vs /16) is normally preferred when multiple routes could match — called the \"longest prefix match\" rule.\n- If a router has no matching route for a destination, it typically forwards to a \"default route,\" or drops the packet if none exists.\n\nComplexity / performance:\nA single routing table lookup is typically O(log n) using a trie or similar structure, regardless of the total number of hops the packet still has to travel.\n\nReal-world relevance:\nEvery time you `traceroute` a website, you're watching exactly this hop-by-hop routing decision-making happen in real time, one router at a time.\n\n"
