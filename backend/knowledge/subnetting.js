// Knowledge base entry for the chatbot — Subnetting (Computer Networks)
export const content = "Subnetting (Computer Networks)\n==============================\n\nWhat it is:\nSubnetting is dividing a single, larger IP network into multiple smaller networks (subnets) by extending the network prefix, borrowing bits from what was previously the host portion.\n\nWhy it matters:\nA flat /24 network puts every device in one broadcast domain, which doesn't scale and doesn't match how organizations are actually structured (different floors, departments, or security zones). Subnetting creates smaller, isolated, more manageable networks from one address block.\n\nHow it works:\nBorrowing n bits from the host portion creates 2ⁿ subnets, each with 2^(remaining host bits) addresses. Each subnet reserves its first address as the network address and its last as the broadcast address — neither is assignable to a host.\n\nWhat to observe:\nWatch exactly which bits of the last octet change between subnets (the 2 leftmost bits of that octet) while the remaining 6 bits still range across the full 00000000–11111111 span within each subnet.\n\nOutcome:\nFour independent /26 subnets, each covering exactly 64 addresses (62 usable), carved cleanly out of the original 256-address /24 block.\n\nImportant points:\n- Borrowing more bits creates more, smaller subnets; borrowing fewer bits creates fewer, larger subnets — it's a direct tradeoff.\n- The network address and broadcast address of every subnet are never assignable to a host — that's why usable hosts = block size − 2.\n- CIDR notation (/26) is a compact way of expressing exactly the subnet mask (255.255.255.192 in this case) used in this calculation.\n\nComplexity / performance:\nAll subnetting math is fixed 32-bit arithmetic — O(1) regardless of network size — though planning which departments get which subnet is a design decision, not a computed one.\n\nReal-world relevance:\nNetwork engineers subnet address blocks constantly — separating a guest Wi-Fi network from an internal office network, or isolating a company's different departments, are both classic subnetting use cases.\n\n"
