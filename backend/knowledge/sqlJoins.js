// Knowledge base entry for the chatbot — SQL JOINs (DBMS)
export const content = "SQL JOINs (DBMS)\n================\n\nWhat it is:\nA SQL JOIN combines rows from two tables based on a related column between them — here, Students.courseId matching Courses.id.\n\nWhy it matters:\nReal data is normalized across multiple tables to avoid duplication. JOINs let you ask questions that span tables, like \"which course is each student enrolled in?\", without storing course details inside the Students table itself.\n\nHow it works:\nFor each row on the driving side, the engine looks for row(s) on the other side where the join condition holds. INNER JOIN keeps only matched pairs; LEFT/RIGHT keep every row from one side and pad the other with NULLs when there's no match; FULL keeps every row from both sides.\n\nWhat to observe:\nWatch which table is scanned first, which rows get a green match highlight versus a red \"no match\", and how the result table grows one row at a time as each decision is made.\n\nOutcome:\nA single result table combining columns from both sources, whose row count and NULL-padding depend entirely on which join type was selected.\n\nImportant points:\n- INNER JOIN is the strictest — it can silently drop real rows if the join key doesn't line up, which is a common source of \"missing data\" bugs.\n- LEFT/RIGHT JOIN are direction-sensitive: swapping the table order changes which side gets padded with NULLs.\n- FULL OUTER JOIN is not supported by every database engine (e.g. MySQL emulates it with a UNION of LEFT and RIGHT joins).\n\nComplexity / performance:\nA naive nested-loop join compares every row on one side against every row on the other — O(n·m). Real databases avoid this using indexes or hash joins, often bringing it close to O(n + m).\n\nReal-world relevance:\nAlmost every non-trivial report or API endpoint backed by a relational database relies on joins — e.g. \"orders with customer names\" or \"posts with author details\" are both joins under the hood.\n\n"
