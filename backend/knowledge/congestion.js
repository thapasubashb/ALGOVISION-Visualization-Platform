// Knowledge base entry for the chatbot — TCP Congestion Control (Computer Networks)
export const content = "TCP Congestion Control (Computer Networks)\n==========================================\n\nWhat it is:\nTCP congestion control dynamically adjusts how much unacknowledged data (the congestion window, cwnd) a sender is allowed to have in flight, to avoid overwhelming the network.\n\nWhy it matters:\nA sender has no direct visibility into how congested the network path is. Sending too fast causes packet loss and buffer overflow at routers; sending too slow wastes available bandwidth. TCP needs an adaptive strategy that works without global network knowledge.\n\nHow it works:\nSlow start grows cwnd exponentially at first (doubling each round trip) to quickly find a rough capacity estimate. Once cwnd passes ssthresh, congestion avoidance grows it only linearly (+1 per round trip) to probe more cautiously. On packet loss, ssthresh is halved and cwnd drops, restarting the cautious growth.\n\nWhat to observe:\nWatch the shape of the graph: a steep exponential curve during slow start, a shallower straight line during congestion avoidance, and a sharp vertical drop the instant a loss is detected.\n\nOutcome:\nSending rate rises quickly at first, then grows more cautiously, and pulls back sharply on any sign of congestion — repeating this cycle for the life of the connection.\n\nImportant points:\n- The repeating rise-then-drop shape is called the TCP \"sawtooth\" and is one of the most recognizable patterns in networking.\n- Modern TCP variants (like CUBIC, used by default on Linux) tune this curve differently, but the slow-start/congestion-avoidance/loss-response structure remains conceptually similar.\n- This is why a single very lossy link can dramatically reduce a TCP connection's effective throughput — every loss event is a forced cwnd cut.\n\nComplexity / performance:\nSlow start reaches a cwnd of size n in O(log n) round trips (exponential growth); congestion avoidance takes O(n) round trips to grow by the same amount (linear growth) — a deliberate asymmetry between \"probe fast\" and \"back off cautiously\".\n\nReal-world relevance:\nEvery TCP-based download, video stream, or file transfer you've ever done had its throughput shaped by exactly this algorithm running invisibly underneath.\n\n"
