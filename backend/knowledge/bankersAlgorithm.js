// Knowledge base entry for the chatbot — Banker's Algorithm (Operating Systems)
export const content = "Banker's Algorithm (Operating Systems)\n======================================\n\nWhat it is:\nBanker's Algorithm is a deadlock-avoidance algorithm that checks, before granting a resource request, whether the resulting state is still \"safe\" — meaning every process could still finish given enough time.\n\nWhy it matters:\nDeadlock detection (see the Deadlocks topic) reacts after a deadlock has already happened. Banker's Algorithm instead prevents it proactively — the OS simulates granting a request and only actually grants it if the system would remain in a safe state.\n\nHow it works:\nFor each process, compute Need = Max − Allocation. Repeatedly scan for any not-yet-finished process whose Need can be fully satisfied by the currently Available resources; \"run\" it (add its Allocation back to Available) and mark it done. If every process eventually finishes this way, a safe sequence exists.\n\nWhat to observe:\nWatch how a process that initially can't run (its Need exceeds Available) becomes runnable later, once an earlier process finishes and returns its resources to the pool — that's exactly why we need to keep retrying processes in a loop.\n\nOutcome:\nA safe sequence (P1 → P3 → P4 → P0 → P2 in this classic example) is found, confirming the current allocation state is safe and won't lead to deadlock.\n\nImportant points:\n- A safe state guarantees no deadlock; but an unsafe state doesn't guarantee deadlock will happen — it just means the algorithm can't prove safety.\n- The algorithm requires knowing each process's maximum possible resource demand in advance — a strong assumption that limits its use in some real systems.\n- The safety-check part of the algorithm is run every time a new resource request comes in, not just once at startup.\n\nComplexity / performance:\nThe safety algorithm is O(n² × m) for n processes and m resource types — each of the n passes may scan all remaining processes, each requiring an O(m) comparison.\n\nReal-world relevance:\nBanker's Algorithm is more often taught as a foundational concept than deployed literally in modern OSes (its need-to-know-max-demand requirement is impractical for general-purpose systems), but its core idea — check safety before granting — underlies many real resource managers.\n\n"
