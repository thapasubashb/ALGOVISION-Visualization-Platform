// Knowledge base entry for the chatbot — Process States (Operating Systems)
export const content = "Process States (Operating Systems)\n==================================\n\nWhat it is:\nEvery process an operating system manages moves through a well-defined set of states: New, Ready, Running, Waiting (Blocked), and Terminated.\n\nWhy it matters:\nThe OS needs a clear model of what every process is currently capable of doing, so its scheduler can make correct decisions — you can't dispatch a Waiting process to the CPU, and you shouldn't let a Terminated one hold onto memory.\n\nHow it works:\nTransitions are triggered by specific events: admission (New → Ready), scheduling (Ready → Running), an I/O or event request (Running → Waiting), an I/O or event completion (Waiting → Ready), and either normal completion or being killed (Running → Terminated).\n\nWhat to observe:\nNotice that a process can bounce between Ready and Running many times (via preemption or I/O) before ever reaching Terminated — the diagram is a cycle, not a straight line, except for the final Terminated state.\n\nOutcome:\nThis particular process cycles through Ready/Running twice (once blocking for I/O in between) before finally terminating.\n\nImportant points:\n- Running → Ready (without Waiting) also happens on preemption — e.g. a time quantum expiring in Round Robin.\n- A process can only be Running on one CPU core at a time, but many processes can be Ready or Waiting simultaneously.\n- Some textbooks add a \"Suspended\" state for processes swapped out to disk under memory pressure — this simulation covers the five-state core model.\n\nComplexity / performance:\nState transitions themselves are O(1) — the OS just updates the PCB and moves it between queues; the real cost lies in the scheduling decision of which Ready process runs next.\n\nReal-world relevance:\nEvery \"Not Responding\" app on your machine is stuck in some observable state — often blocked waiting on I/O or a lock — and OS tools like `top`/Task Manager literally show you a live snapshot of every process's current state.\n\n"
