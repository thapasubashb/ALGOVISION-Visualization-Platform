// Knowledge base entry for the chatbot — SJF / SRTF Scheduling (Operating Systems)
export const content = "SJF / SRTF Scheduling (Operating Systems)\n=========================================\n\nWhat it is:\nSJF (Shortest Job First) always picks the process with the smallest total burst time to run next. SRTF (Shortest Remaining Time First) is its preemptive version — it can interrupt a running process the instant a shorter one becomes available.\n\nWhy it matters:\nRunning shorter jobs first minimizes average waiting time — a mathematically proven optimal result for non-preemptive scheduling when all burst times are known in advance. SRTF pushes this further by reacting immediately to new, shorter arrivals.\n\nHow it works:\nAt every moment, among all arrived-but-unfinished processes, SRTF runs whichever has the least remaining burst time. If a newly arrived process has a strictly shorter remaining time than whatever is currently running, the CPU immediately switches to it (preemption).\n\nWhat to observe:\nWatch P1 get preempted right after P3 arrives with a shorter remaining time, and resume much later — its execution is split into two separate Gantt segments, unlike FCFS where every process runs as one unbroken block.\n\nOutcome:\nA noticeably lower average waiting time than FCFS on the identical workload, at the cost of P1 experiencing a mid-execution preemption.\n\nImportant points:\n- SRTF requires knowing (or estimating) burst times in advance — real systems often estimate this from a process's recent history rather than knowing it exactly.\n- SRTF can starve long processes if short jobs keep arriving — a very long process might wait indefinitely in a worst case.\n- This preemptive requirement means every arrival is a potential scheduling decision point, unlike FCFS which only decides when the CPU goes idle.\n\nComplexity / performance:\nSelecting the shortest remaining job is O(log n) per decision using a priority queue (min-heap), with a scheduling decision needed at every arrival and completion event.\n\nReal-world relevance:\nInteractive systems favoring short, quick tasks (like handling a keystroke) over long background jobs (like a file compression) borrow directly from this same shortest-job-first intuition.\n\n"
