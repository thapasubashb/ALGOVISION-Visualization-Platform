// Knowledge base entry for the chatbot — FIFO Page Replacement (Operating Systems)
export const content = "FIFO Page Replacement (Operating Systems)\n=========================================\n\nWhat it is:\nFIFO (First-In, First-Out) page replacement evicts the page that has been in physical memory the longest, when a new page needs to be loaded and no free frame is available.\n\nWhy it matters:\nPhysical memory (RAM) is limited, but programs can reference far more virtual pages than fit at once. When a needed page isn't resident (a \"page fault\"), the OS must choose an existing page to evict — FIFO is the simplest possible policy for that choice.\n\nHow it works:\nThe OS maintains a queue of pages in the order they were loaded. On a page fault with no free frames, the page at the front of the queue (the oldest resident page) is evicted, and the new page is loaded and pushed to the back of the queue.\n\nWhat to observe:\nWatch that eviction is based purely on load order — a page that was just used a moment ago can still be evicted if it happened to load in early, which is exactly what happens to page 0 partway through this trace.\n\nOutcome:\n8 references produce several page faults under FIFO — notice this count so you can compare it directly against LRU's result on the identical reference string.\n\nImportant points:\n- FIFO can suffer from Bélády's anomaly — a strange case where adding MORE frames actually increases the number of page faults, which shouldn't happen intuitively.\n- FIFO is simple and cheap to implement (just a queue) but ignores actual usage patterns, which usually makes it perform worse than LRU in practice.\n- The very first accesses to any page are always faults, regardless of algorithm — those are called \"compulsory misses.\"\n\nComplexity / performance:\nEach page reference is handled in O(1) — checking frame membership and updating the queue are both constant-time operations (with appropriate data structures).\n\nReal-world relevance:\nReal operating systems rarely use pure FIFO because of its poor real-world performance and Bélády's anomaly — but it remains an essential teaching baseline for understanding paging.\n\n"
