// Knowledge base entry for the chatbot — HTTP / HTTPS (Computer Networks)
export const content = "HTTP / HTTPS (Computer Networks)\n================================\n\nWhat it is:\nHTTP (HyperText Transfer Protocol) is the request/response protocol browsers and servers use to exchange web content. HTTPS is HTTP layered on top of TLS encryption.\n\nWhy it matters:\nPlain HTTP sends everything — including passwords, cookies, and form data — as readable text over the network. HTTPS adds a TLS handshake that encrypts the entire exchange, protecting it from eavesdroppers on the network path.\n\nHow it works:\nBoth start with DNS resolution and a TCP connection. HTTPS inserts one extra step — a TLS handshake — before any HTTP data is exchanged. After that, both protocols follow the same request → server processing → response pattern, just with HTTPS encrypting every byte.\n\nWhat to observe:\nToggle between HTTP and HTTPS and watch for the extra TLS handshake step, and notice which steps turn into a \"locked\" (encrypted) visual once TLS is active.\n\nOutcome:\nBoth modes render the same page in the browser — but HTTPS protects everything exchanged along the way from network eavesdroppers, at the cost of one extra round trip during the TLS handshake.\n\nImportant points:\n- HTTPS uses port 443 by default; plain HTTP uses port 80.\n- The TLS handshake overhead is a one-time cost per connection — modern browsers and servers reuse (keep-alive) connections to amortize it across many requests.\n- A padlock icon means the connection is encrypted — it does NOT guarantee the site itself is trustworthy, only that the data in transit can't be read by a third party.\n\nComplexity / performance:\nThe TLS handshake adds roughly one extra round trip (TLS 1.3) compared to plain HTTP's TCP handshake alone — a small, fixed, one-time cost per new connection.\n\nReal-world relevance:\nModern browsers actively warn users (\"Not Secure\") when a site uses plain HTTP, and most of the web has moved to HTTPS by default — this exact difference is why.\n\n"
