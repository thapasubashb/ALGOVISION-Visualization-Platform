// Knowledge base entry for the chatbot — ARP (Computer Networks)
export const content = "ARP (Computer Networks)\n=======================\n\nWhat it is:\nARP (Address Resolution Protocol) maps a known IP address to the unknown MAC (hardware) address of the device that owns it, on a local network.\n\nWhy it matters:\nIP addresses are logical and used for routing across networks, but the actual hardware (Ethernet/Wi-Fi) delivers frames using MAC addresses. Before a device can send a frame to a given IP on its local network, it must first learn that IP's corresponding MAC address.\n\nHow it works:\nThe requesting device broadcasts an ARP request (\"who has this IP?\") to every device on the local network. Only the device that owns that IP replies, directly (unicast), with its MAC address. The requester caches this mapping for future use.\n\nWhat to observe:\nNotice the asymmetry: the request is a broadcast (everyone hears it), but the reply is a unicast (only the original requester hears it) — and only the actual owner of the IP responds at all.\n\nOutcome:\nPC A's ARP cache now has a fresh entry mapping 192.168.1.20 to PC B's MAC address, letting future frames skip the broadcast step.\n\nImportant points:\n- ARP only works within a single local network (a broadcast domain) — it can't resolve an IP address on a different network. That's the router's job.\n- ARP cache entries expire after a timeout (commonly a few minutes) so the cache stays accurate as devices change or leave the network.\n- ARP spoofing (a malicious host claiming to own someone else's IP) is a real security concern, since ARP has no built-in authentication.\n\nComplexity / performance:\nA cache hit is O(1). A cache miss costs one broadcast round-trip, regardless of how many hosts are on the network (everyone processes it in parallel).\n\nReal-world relevance:\nEvery device on your home Wi-Fi resolves your router's IP to its MAC address via ARP the moment it joins the network — you can see these entries yourself with the `arp -a` command.\n\n"
