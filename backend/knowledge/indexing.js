// Knowledge base entry for the chatbot — Indexing (DBMS)
export const content = "Indexing (DBMS)\n===============\n\nWhat it is:\nAn index is an auxiliary data structure (commonly a B-tree) that maps column values to the physical location of the matching rows, letting the database find rows without checking every one.\n\nWhy it matters:\nWithout an index, any WHERE clause on a large table forces a full table scan — checking every row, one by one. As tables grow to millions of rows, that becomes far too slow for interactive queries.\n\nHow it works:\nThe database maintains a sorted structure of (indexed value → row pointer) pairs. A lookup uses binary search (or B-tree traversal) on that structure instead of scanning the table itself, jumping straight to the relevant rows.\n\nWhat to observe:\nCompare the comparison counters between the two runs — the scan's counter climbs by exactly 1 per row until it hits the match, while the index's counter roughly halves the remaining search space every single step.\n\nOutcome:\nBoth approaches find the same row, but the index does it in far fewer comparisons — and that gap widens dramatically as the table grows.\n\nImportant points:\n- Indexes speed up reads but add overhead to writes — every INSERT/UPDATE/DELETE must also update the index.\n- An index only helps if the query actually filters or sorts on the indexed column(s) — an index on `name` won't help a query filtering on `id`.\n- Primary keys and UNIQUE columns get an index automatically in most databases; other columns need an explicit CREATE INDEX.\n\nComplexity / performance:\nA full scan is O(n). An index lookup (via a balanced structure) is O(log n) — for a million rows that's roughly 1,000,000 vs about 20 comparisons.\n\nReal-world relevance:\nSlow queries in production are extremely often solved by adding the right index — it's usually the single highest-leverage database performance fix available.\n\n"
