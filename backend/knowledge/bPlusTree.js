// Knowledge base entry for the chatbot — B+ Tree (DBMS)
export const content = "B+ Tree (DBMS)\n==============\n\nWhat it is:\nA B+ Tree is a self-balancing, sorted tree structure where every value lives in a leaf node, internal nodes only hold routing keys, and all leaves sit at the same depth and are linked together in order.\n\nWhy it matters:\nDatabases store far more data than fits in memory. A B+ tree keeps the tree shallow (high fanout) so finding any row takes only a handful of disk reads, and its sorted leaf-linked-list makes range queries (e.g. \"id BETWEEN 10 AND 30\") fast too.\n\nHow it works:\nInsert descends from the root, following the child whose key range contains the new key, until it reaches a leaf. The key is inserted in sorted order; if the leaf now has too many keys, it splits into two, and a copy of the separator key moves up into the parent — which can itself split, if needed.\n\nWhat to observe:\nWatch how inserting 5 and then 6 both trigger a leaf split — and how the second split doesn't create a new root (a parent already exists to absorb it), while the very first split had no parent yet, so a brand new root was created.\n\nOutcome:\nA tree with one root routing key and three linked leaves, all containing every inserted key in sorted order — and a search that reaches the correct leaf in just 2 comparisons instead of scanning all 6 keys.\n\nImportant points:\n- Only leaves hold \"real\" data pointers — internal node keys exist purely to route searches, which is what distinguishes a B+ tree from a plain B-tree.\n- This example uses order 3 (max 2 keys/node) to keep splits frequent and visible; real database indexes use a much higher order (often 100+) to keep the tree extremely shallow.\n- Leaves are linked left-to-right, which is what makes range scans fast — you don't need to revisit the root between leaves.\n\nComplexity / performance:\nSearch, insert and delete are all O(log n) — and because the fanout is high, that logarithm has a very large base, so real B+ trees stay just 3-4 levels deep even with millions of rows.\n\nReal-world relevance:\nThis is exactly the structure behind a SQL PRIMARY KEY or indexed column in MySQL (InnoDB) and most other relational databases — \"the index\" you create with CREATE INDEX is almost always a B+ tree under the hood.\n\n"
